<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="edu.gatech.convertor.MainActivity$PlaceholderFragment" >

    <Button
        android:id="@+id/clickDist"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
         android:layout_below="@+id/clickDist"
        android:layout_marginTop="60dp"
        android:text="Click to go to Distance Convertor" 
        android:onClick="handleClick"
        android:textSize="20dip"/>

    <Button
        android:id="@+id/clickTemp"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/clickDist"
        android:layout_below="@+id/clickDist"
        android:onClick="handleClick1"
        android:text="Click to go to Temperature Convertor"
        android:textSize="20dip" />

    <Button
        android:id="@+id/clickWeight"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/clickTemp"
        android:layout_below="@+id/clickTemp"
        android:onClick="handleClick2"
        android:text="Click to go to Weight Convertor"
        android:textSize="20dip" />

</RelativeLayout>
